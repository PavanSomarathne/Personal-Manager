<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.organizer">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.example.organizer.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name="com.example.organizer.GalleryEdit" />
        <activity android:name="com.example.organizer.Gallery" />
        <activity android:name="com.example.organizer.FriendListSearch" />
        <activity android:name="com.example.organizer.FriendEdit" />
        <activity android:name="com.example.organizer.AddFriend" />
        <activity android:name="com.example.organizer.TODOEdit" />
        <activity android:name="com.example.organizer.TODOSearch" />
        <activity android:name="com.example.organizer.TODOList" />
        <activity android:name="com.example.organizer.ManageTODO" />
        <activity android:name="com.example.organizer.AddTODO" />
        <activity android:name="com.example.organizer.EventSearch" />
        <activity android:name="com.example.organizer.EventEdit" />
        <activity android:name="com.example.organizer.ManageEvent" />
        <activity android:name=".payment" />
        <activity android:name=".bid" />
        <activity android:name=".search" />
        <activity android:name="com.example.organizer.EventList" />
        <activity android:name="com.example.organizer.AddEvent" />
        <activity android:name=".auctionAdd" />
        <activity android:name="com.example.organizer.ManageFriends" />
        <activity android:name="com.example.organizer.ManageGallery" />
        <activity android:name="com.example.organizer.AddImage" />
        <activity android:name="com.example.organizer.FriendList" />
        <activity android:name="com.example.organizer.SignUpActivity" />
        <activity android:name="com.example.organizer.LoginActivity">

            <!-- android:label="@string/title_activity_login" -->
            <!-- android:parentActivityName=".HomeActivity"> -->
            <!-- <meta-data -->
            <!-- android:name="android.support.PARENT_ACTIVITY" -->
            <!-- android:value="com.example.pictza.HomeActivity" /> -->
        </activity>
        <activity android:name="com.example.organizer.HomeActivity" />
        <activity android:name="com.example.organizer.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>