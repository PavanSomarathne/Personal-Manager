<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pictza">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".GalleryEdit" />
        <activity android:name=".Gallery" />
        <activity android:name=".FriendListSearch" />
        <activity android:name=".FriendEdit" />
        <activity android:name=".AddFriend" />
        <activity android:name=".UpdateTODO" />
        <activity android:name=".TODOSearch" />
        <activity android:name=".TODOView" />
        <activity android:name=".AddOrCart" />
        <activity android:name=".ManageCart" />
        <activity android:name=".AddTODO" />
        <activity android:name=".EventSearch" />
        <activity android:name=".PaintingEdit" />
        <activity android:name=".paintings" />
        <activity android:name=".FriendEditDelete" />
        <activity android:name=".payment" />
        <activity android:name=".bid" />
        <activity android:name=".search" />
        <activity android:name=".ManageEvent" />
        <activity android:name=".UploadPainting" />
        <activity android:name=".ManageFriends" />
        <activity android:name=".ManageGallery" />
        <activity android:name=".AddImage" />
        <activity android:name=".FriendList" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity">

            <!-- android:label="@string/title_activity_login" -->
            <!-- android:parentActivityName=".HomeActivity"> -->
            <!-- <meta-data -->
            <!-- android:name="android.support.PARENT_ACTIVITY" -->
            <!-- android:value="com.example.pictza.HomeActivity" /> -->
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>